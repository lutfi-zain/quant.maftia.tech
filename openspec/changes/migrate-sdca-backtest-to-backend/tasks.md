## 1. Python Backend Engine

- [x] 1.1 Extract SDCA backtest logic from frontend and create a new Python script (e.g., `calculate_sdca_backtest.py`) that reads `unified_daily_analytics`.
- [x] 1.2 Ensure the Python script implements the true sign convention (Positive = Buy, Negative = Sell) and the aggressive DD protection multipliers (-5.0, -10.0, -20.0).
- [x] 1.3 Update the Python script to export the daily equity curve, metrics, and trade logs to a static JSON file (e.g., `data/sdca_backtest.json`) for fast serving.
- [x] 1.4 Integrate the new script into the master pipeline `run_report_pipeline.py`.

## 2. API Gateway

- [x] 2.1 Add a new route `GET /api/v1/backtest/sdca` in the Hono API Gateway (`src/api/` or similar).
- [x] 2.2 Configure the route to read and serve the `sdca_backtest.json` artifact generated by the pipeline.

## 3. Frontend Integration

- [x] 3.1 Refactor `web/src/components/studios/ValuationStudio.tsx` to fetch backtest data from the new Hono API route.
- [x] 3.2 Update data bindings in Lightweight Charts to consume the API payload, maintaining the strict 85px Y-axis lock and vertical crosshair synchronization.
- [x] 3.3 Deprecate and remove local SDCA calculations from `web/src/lib/studioBacktest.ts` and `web/src/lib/sdcaEngine.ts`.

## 4. Verification & Testing

- [ ] 4.1 Run automated pipeline verification using `python3 /home/ubuntu/projects/run_report_pipeline.py` to confirm data generation succeeds.
- [ ] 4.2 Start the web and API servers and visually verify that Valuation Studio loads the charts correctly with aligned markers and trade logs.
- [ ] 4.3 Commit changes using Conventional Commits (`quant: migrate SDCA backtest to backend`, etc).
