## 1. Marker Taxonomy & Component Updates

- [x] 1.1 Implement marker layer selector in `web/src/components/studios/ValuationStudio.tsx` (`Strategy Orders`, `Macro Confluence`, `All Markers`)
- [x] 1.2 Unify marker styling and colors across `MultiPaneChart.tsx` and `ValuationStudio.tsx` to maintain consistent visual taxonomy

## 2. Verification

- [x] 2.1 Verify chart marker rendering and performance on desktop and mobile viewports (`cd web && bun run build`)
- [x] 2.2 Run Playwright/unit tests to ensure zero regressions in chart interactions
